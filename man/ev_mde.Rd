% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{ev_mde}
\alias{ev_mde}
\title{Smallest Difference an Evaluation Can Detect}
\usage{
ev_mde(
  n_items,
  sd_diff = NULL,
  pilot = NULL,
  p_a = NULL,
  p_b = NULL,
  correlation = NULL,
  power = 0.8,
  alpha = 0.05,
  icc = 0,
  cluster_size = 1
)
}
\arguments{
\item{n_items}{Number of questions available.}

\item{sd_diff}{Standard deviation of the per-item difference.}

\item{pilot}{A result from \code{\link[=ev_paired]{ev_paired()}} to take \code{sd_diff} from.}

\item{p_a, p_b}{Expected scores of the two models, for binary grading.}

\item{correlation}{Expected correlation between the two models' item scores.
Used with \code{p_a} and \code{p_b}. Default \code{0}, which is conservative.}

\item{power}{Target power. Default \code{0.8}.}

\item{alpha}{Two-sided significance level. Default \code{0.05}.}

\item{icc}{Intra-cluster correlation, from \code{\link[=ev_icc]{ev_icc()}}. Default \code{0}.}

\item{cluster_size}{Questions per cluster. Default \code{1}, meaning no clustering.}
}
\value{
An \code{evaluatellm_mde} object with elements \code{mde}, \code{n_items},
\code{n_effective}, \code{sd_diff}, \code{power}, \code{alpha}, \code{design_effect}, and \code{icc}.
}
\description{
The mirror of \code{\link[=ev_power]{ev_power()}}. Given the number of questions available, returns
the smallest true difference the evaluation has a decent chance of detecting.
}
\details{
Run this before an evaluation, not after. If the minimum detectable effect
comes back larger than the gain you expect, the evaluation cannot answer the
question and a null result will mean nothing. Reporting the minimum
detectable effect alongside a null finding is what separates "the models are
equivalent" from "this evaluation was too small to tell".
}
\examples{
# 500 questions, binary scoring, models correlated at 0.7
ev_mde(n_items = 500, p_a = 0.72, p_b = 0.70, correlation = 0.7)

# Same questions, but clustered 8 to a passage: the MDE nearly doubles
ev_mde(n_items = 500, p_a = 0.72, p_b = 0.70, correlation = 0.7,
       icc = 0.25, cluster_size = 8)

}
\seealso{
Other planning: 
\code{\link{ev_power}()}
}
\concept{planning}
