% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{ev_bootstrap}
\alias{ev_bootstrap}
\title{Cluster Bootstrap for an Arbitrary Statistic}
\usage{
ev_bootstrap(
  data,
  model = NULL,
  statistic = mean,
  R = 2000,
  level = 0.95,
  type = c("percentile", "basic"),
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{An \code{evaluatellm_eval} object from \code{\link[=as_eval]{as_eval()}}, a data frame, or a
bare numeric or logical vector of scores.}

\item{model}{Which model to score. Optional when the data holds only one.}

\item{statistic}{A function taking a numeric vector of item scores and
returning a single number. Default \code{\link[=mean]{mean()}}.}

\item{R}{Number of bootstrap replicates. Default \code{2000}.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{type}{Interval type, \code{"percentile"} (default) or \code{"basic"}.}

\item{seed}{Optional integer seed for reproducibility.}

\item{...}{Passed to \code{\link[=as_eval]{as_eval()}} when \code{data} is a plain data frame.}
}
\value{
An \code{evaluatellm_bootstrap} object with elements \code{estimate}, \code{se},
\code{conf_low}, \code{conf_high}, \code{replicates}, \code{R}, \code{type}, and \code{level}.
}
\description{
Resamples items, or whole clusters of items when a cluster column is present,
and recomputes a user-supplied statistic on each replicate. Use this when the
quantity of interest is not a mean and the analytic standard errors elsewhere
in the package do not apply: medians, quantiles, pass rates above a
threshold, or any custom score aggregation.
}
\details{
Resampling clusters rather than items preserves the dependence structure, so
the resulting interval carries the same protection as \code{\link[=ev_cluster]{ev_cluster()}}.
}
\examples{
set.seed(5)
e <- as_eval(rbeta(300, 6, 3))
ev_bootstrap(e, statistic = median, R = 500, seed = 1)

}
\seealso{
Other single model: 
\code{\link{ev_cluster}()},
\code{\link{ev_icc}()},
\code{\link{ev_resample}()},
\code{\link{ev_score}()}
}
\concept{single model}
