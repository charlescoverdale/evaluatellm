% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paired.R
\name{ev_unpaired}
\alias{ev_unpaired}
\title{Unpaired Comparison of Two Models}
\usage{
ev_unpaired(data, model_a = NULL, model_b = NULL, level = 0.95, ...)
}
\arguments{
\item{data}{An \code{evaluatellm_eval} object holding both models, or a data frame
passed to \code{\link[=as_eval]{as_eval()}} along with \code{...}.}

\item{model_a}{Name of the first model. The reported difference is
\code{model_a - model_b}.}

\item{model_b}{Name of the second model.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{...}{Passed to \code{\link[=as_eval]{as_eval()}} when \code{data} is a plain data frame.}
}
\value{
An \code{evaluatellm_unpaired} object with elements \code{estimate}, \code{se},
\code{conf_low}, \code{conf_high}, \code{statistic}, \code{p_value}, \code{mean_a}, \code{mean_b},
\code{n_a}, \code{n_b}, \code{df}, and \code{level}.
}
\description{
Compares two models evaluated on different question sets, using a Welch
two-sample interval that does not assume equal variances.
}
\details{
Prefer \code{\link[=ev_paired]{ev_paired()}} whenever both models were run on the same questions.
This function exists for the case where they were not, for instance when
comparing a published score against your own run, and it will be
substantially less powerful because item difficulty is left in the error
term.
}
\examples{
set.seed(7)
d <- data.frame(
  q = 1:600,
  m = rep(c("a", "b"), each = 300),
  correct = rbinom(600, 1, rep(c(0.72, 0.65), each = 300))
)
ev_unpaired(as_eval(d, score = correct, item = q, model = m),
            model_a = "a", model_b = "b")

}
\seealso{
Other comparison: 
\code{\link{ev_multi}()},
\code{\link{ev_paired}()},
\code{\link{ev_variance_reduction}()}
}
\concept{comparison}
