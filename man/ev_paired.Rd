% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paired.R
\name{ev_paired}
\alias{ev_paired}
\title{Paired Comparison of Two Models}
\usage{
ev_paired(
  data,
  model_a = NULL,
  model_b = NULL,
  level = 0.95,
  cluster = TRUE,
  ...
)
}
\arguments{
\item{data}{An \code{evaluatellm_eval} object holding both models, or a data frame
passed to \code{\link[=as_eval]{as_eval()}} along with \code{...}.}

\item{model_a}{Name of the first model. The reported difference is
\code{model_a - model_b}.}

\item{model_b}{Name of the second model.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{cluster}{Logical. Use cluster-robust standard errors when a cluster
column is present. Default \code{TRUE}.}

\item{...}{Passed to \code{\link[=as_eval]{as_eval()}} when \code{data} is a plain data frame.}
}
\value{
An \code{evaluatellm_paired} object with elements \code{estimate}, \code{se},
\code{se_unpaired}, \code{conf_low}, \code{conf_high}, \code{statistic}, \code{p_value},
\code{correlation}, \code{variance_reduction}, \code{mean_a}, \code{mean_b}, \code{n_items}, \code{df},
and \code{level}.
}
\description{
Tests whether one model outscores another on the same set of questions. This
is the correct comparison whenever both models were run on the same
evaluation, and it is materially more powerful than comparing two independent
means.
}
\details{
Working with the per-item difference \code{d_i = score_A - score_B} removes the
item difficulty that both models face, so the variance of the difference is
\code{var_A + var_B - 2 * cov(A, B)} rather than \code{var_A + var_B}. Because model
scores on shared questions are usually strongly correlated, the paired
standard error is routinely half the unpaired one or better, which is the
difference between detecting a real gain and reporting a null result. The
function reports the realised gain as \code{variance_reduction}.

When the data carry a cluster column the standard error of the mean
difference is cluster-robust, and the test uses \code{G - 1} degrees of freedom.
}
\examples{
set.seed(6)
# Shared item difficulty makes the two models' scores correlate
difficulty <- rnorm(500)
d <- data.frame(
  q = rep(1:500, 2),
  m = rep(c("new", "old"), each = 500),
  correct = rbinom(1000, 1,
    plogis(c(0.9, 0.7)[rep(1:2, each = 500)] - rep(difficulty, 2)))
)
ev_paired(as_eval(d, score = correct, item = q, model = m),
          model_a = "new", model_b = "old")

}
\references{
Miller, E. (2024). Adding Error Bars to Evals: A Statistical Approach to
Language Model Evaluations. \doi{10.48550/arXiv.2411.00640}
}
\seealso{
Other comparison: 
\code{\link{ev_multi}()},
\code{\link{ev_unpaired}()},
\code{\link{ev_variance_reduction}()}
}
\concept{comparison}
