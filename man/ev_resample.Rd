% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample.R
\name{ev_resample}
\alias{ev_resample}
\title{Variance Decomposition for Repeated Sampling}
\usage{
ev_resample(
  data,
  model = NULL,
  level = 0.95,
  k_grid = c(1, 2, 4, 8, 16, Inf),
  ...
)
}
\arguments{
\item{data}{An \code{evaluatellm_eval} object with several rows per item and model, or
a data frame passed to \code{\link[=as_eval]{as_eval()}} along with \code{...}.}

\item{model}{Which model to score. Optional when the data holds only one.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{k_grid}{Integer vector of \code{k} values at which to report the projected
standard error. Default \code{c(1, 2, 4, 8, 16, Inf)}.}

\item{...}{Passed to \code{\link[=as_eval]{as_eval()}} when \code{data} is a plain data frame.}
}
\value{
An \code{evaluatellm_resample} object with elements \code{estimate}, \code{se},
\code{se_floor}, \code{var_between}, \code{var_within}, \code{share_within}, \code{n_items},
\code{k_mean}, \code{projection}, \code{df}, and \code{level}.
}
\description{
When an evaluation draws several responses per question, the observed spread
of question means mixes two sources: genuine variation between questions, and
sampling noise in the model's own responses. This function separates them,
and reports how far more sampling can take you.
}
\details{
Writing \code{s_ij} for the score of response \code{j} to item \code{i}, the estimator is the
equal-weighted mean of item means. Its variance is \verb{var(item means) / n},
which is unbiased whatever \code{k} is. The decomposition uses
\verb{E[var(item means)] = var_between + E[var_within / k]}, so

\verb{var_between = var(item means) - mean(var_within / k)}

The practical consequence is that more responses per question buys precision
only against the within-question term, and there is a floor at
\code{sqrt(var_between / n)} that no amount of resampling can beat. Reaching below
it requires more questions. Sampling noise can push the estimated between
component below zero, in which case it is reported as zero.
}
\examples{
set.seed(4)
# 150 questions, 8 sampled responses each, item difficulty varies
diff <- plogis(rnorm(150, 0.8, 1.1))
d <- data.frame(
  q       = rep(1:150, each = 8),
  draw    = rep(1:8, times = 150),
  correct = rbinom(1200, 1, rep(diff, each = 8))
)
ev_resample(as_eval(d, score = correct, item = q, sample = draw))

}
\references{
Miller, E. (2024). Adding Error Bars to Evals: A Statistical Approach to
Language Model Evaluations. \doi{10.48550/arXiv.2411.00640}
}
\seealso{
Other single model: 
\code{\link{ev_bootstrap}()},
\code{\link{ev_cluster}()},
\code{\link{ev_icc}()},
\code{\link{ev_score}()}
}
\concept{single model}
