% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/judge.R
\name{ev_judge_power}
\alias{ev_judge_power}
\title{How Many Human Labels a Debiased Evaluation Needs}
\usage{
ev_judge_power(
  n_total,
  correlation = NULL,
  target_se = NULL,
  target_mde = NULL,
  sd_gold = 0.5,
  sd_judge = NULL,
  pilot = NULL,
  power = 0.8,
  alpha = 0.05
)
}
\arguments{
\item{n_total}{Number of items the judge will score.}

\item{correlation}{Correlation between judge and human scores, from a pilot.
A fitted \code{\link[=ev_judge_debias]{ev_judge_debias()}} result may be passed instead via \code{pilot}.}

\item{target_se}{Target standard error for the final estimate. Supply this or
\code{target_mde}.}

\item{target_mde}{Target minimum detectable effect, converted to a standard
error at the given \code{power} and \code{alpha}.}

\item{sd_gold}{Standard deviation of the human scores. Defaults to \code{0.5}, the
maximum for a binary score, which is conservative.}

\item{sd_judge}{Standard deviation of the judge scores. Defaults to \code{sd_gold}.}

\item{pilot}{Optional \code{\link[=ev_judge_debias]{ev_judge_debias()}} result to take \code{correlation},
\code{sd_gold} and \code{sd_judge} from.}

\item{power, alpha}{Used only when \code{target_mde} is supplied. Defaults \code{0.8}
and \code{0.05}.}
}
\value{
An \code{evalkit_judge_power} object with elements \code{n_labels},
\code{n_labels_without_judge}, \code{saving}, \code{achieved_se}, \code{target_se},
\code{correlation}, \code{lambda}, and \code{n_total}.
}
\description{
Sizes the human-labelling budget for an evaluation graded by a model judge.
Given how many items the judge will score and how well it tracks human
labels, returns the number of human labels needed to hit a target precision.
}
\details{
This is the question that actually has a budget attached, since human labels
are the expensive input. The function evaluates the variance of the tuned
prediction-powered estimator across candidate label counts and returns the
smallest one that meets the target, alongside the number of labels that
would have been needed without a judge.

A judge correlated at 0.8 with human labels typically cuts the required
labelling budget by around two thirds. Below about 0.4 the judge is barely
worth the pipeline.
}
\examples{
# 20,000 judge-scored items, judge correlates 0.8 with humans,
# want a standard error of 0.01
ev_judge_power(n_total = 20000, correlation = 0.8, target_se = 0.01)

# A weaker judge needs far more human labels
ev_judge_power(n_total = 20000, correlation = 0.4, target_se = 0.01)

}
\seealso{
Other judge: 
\code{\link{ev_judge_agreement}()},
\code{\link{ev_judge_debias}()}
}
\concept{judge}
