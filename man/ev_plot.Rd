% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{ev_plot}
\alias{ev_plot}
\title{Plot Results with Error Bars}
\usage{
ev_plot(x, ..., reference = NULL, xlab = NULL, main = NULL, col = "#1b365d")
}
\arguments{
\item{x}{An \code{evaluatellm} result, a list of them, or a data frame from
\code{\link[=ev_table]{ev_table()}}.}

\item{...}{Further results, or graphical parameters passed to
\code{\link[graphics:plot.default]{graphics::plot()}}.}

\item{reference}{Optional vertical reference line, for example \code{0} for
differences. Default \code{NULL}, which draws none.}

\item{xlab, main}{Axis and title labels.}

\item{col}{Colour for points and intervals.}
}
\value{
The plotted data frame, invisibly.
}
\description{
Draws the results as a horizontal interval plot, the format this package
exists to make routine. Accepts the same input as \code{\link[=ev_table]{ev_table()}}, and also
plots \code{\link[=ev_rank]{ev_rank()}} and \code{\link[=ev_multi]{ev_multi()}} objects directly.
}
\examples{
set.seed(15)
runs <- lapply(1:5, function(i) {
  d <- data.frame(
    q = rep(1:200, 2),
    m = rep(c("new", "old"), each = 200),
    correct = rbinom(400, 1, rep(c(0.72, 0.68), each = 200))
  )
  ev_paired(as_eval(d, score = correct, item = q, model = m),
            model_a = "new", model_b = "old")
})
names(runs) <- paste0("task ", 1:5)
ev_plot(runs, reference = 0)

}
\seealso{
Other reporting: 
\code{\link{ev_table}()}
}
\concept{reporting}
