% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{ev_table}
\alias{ev_table}
\title{Collect Results into a Table}
\usage{
ev_table(...)
}
\arguments{
\item{...}{One or more \code{evaluatellm} result objects, or a single list of them.
Names, where given, become the \code{label} column.}
}
\value{
A data frame with columns \code{label}, \code{term}, \code{estimate}, \code{se},
\code{conf_low}, \code{conf_high}, \code{p_value}, and \code{n}.
}
\description{
Flattens one or more \code{evaluatellm} results into a plain data frame with a common
set of columns, ready for a paper table or a plot.
}
\examples{
set.seed(14)
d <- data.frame(
  q = rep(1:300, 2),
  m = rep(c("new", "old"), each = 300),
  correct = rbinom(600, 1, rep(c(0.74, 0.68), each = 300))
)
e <- as_eval(d, score = correct, item = q, model = m)
ev_table(
  new = ev_score(e, model = "new"),
  old = ev_score(e, model = "old"),
  gain = ev_paired(e, model_a = "new", model_b = "old")
)

}
\seealso{
Other reporting: 
\code{\link{ev_plot}()}
}
\concept{reporting}
