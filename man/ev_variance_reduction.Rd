% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{ev_variance_reduction}
\alias{ev_variance_reduction}
\title{Variance Reduction with a Reference Model}
\usage{
ev_variance_reduction(
  data,
  model = NULL,
  reference = NULL,
  mu_reference = NULL,
  theta = NULL,
  level = 0.95,
  ...
)
}
\arguments{
\item{data}{An \code{evaluatellm_eval} object holding both models, or a data frame
passed to \code{\link[=as_eval]{as_eval()}} along with \code{...}.}

\item{model}{The model being scored.}

\item{reference}{The reference model used as the control variate.}

\item{mu_reference}{The reference model's known mean score on the full
question bank.}

\item{theta}{Optional fixed coefficient. Default \code{NULL}, meaning the
variance-minimising value is estimated from the data.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{...}{Passed to \code{\link[=as_eval]{as_eval()}} when \code{data} is a plain data frame.}
}
\value{
An \code{evaluatellm_vr} object with elements \code{estimate}, \code{se}, \code{conf_low},
\code{conf_high}, \code{estimate_raw}, \code{se_raw}, \code{theta}, \code{correlation},
\code{variance_reduction}, \code{effective_n}, \code{n_items}, \code{df}, and \code{level}.
}
\description{
Uses a reference model whose score on the full question bank is already known
to shrink the standard error of a new model's score, without changing what is
being estimated.
}
\details{
The idea is the control variate. If a reference model scores \code{z_i} on the same
questions and its population mean \code{mu_reference} is known, then the questions
this evaluation happened to draw can be recognised as easier or harder than
average, and the new model's score corrected accordingly:

\code{estimate = mean(y) - theta * (mean(z) - mu_reference)}

The variance-minimising coefficient is \code{theta = cov(y, z) / var(z)}, and the
resulting variance is \code{var(y) * (1 - rho^2) / n}. The adjustment is unbiased
for any \code{theta}, so nothing is assumed beyond \code{mu_reference} being correct.
Because model scores on shared questions correlate strongly, a correlation of
0.8 removes roughly two thirds of the variance, which is equivalent to
tripling the number of questions.

\code{mu_reference} has to come from outside this evaluation: a reference model run
over the whole bank, or a published score on the full benchmark. Supplying the
reference model's mean on these same questions would make the correction
identically zero.
}
\examples{
set.seed(8)
difficulty <- rnorm(200)
d <- data.frame(
  q = rep(1:200, 2),
  m = rep(c("new", "ref"), each = 200),
  correct = rbinom(400, 1,
    plogis(c(1.0, 0.6)[rep(1:2, each = 200)] - rep(difficulty, 2)))
)
e <- as_eval(d, score = correct, item = q, model = m)
# The reference model is known to score 0.63 on the full bank
ev_variance_reduction(e, model = "new", reference = "ref",
                      mu_reference = 0.63)

}
\seealso{
Other comparison: 
\code{\link{ev_multi}()},
\code{\link{ev_paired}()},
\code{\link{ev_unpaired}()}
}
\concept{comparison}
