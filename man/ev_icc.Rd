% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{ev_icc}
\alias{ev_icc}
\title{Intra-Cluster Correlation}
\usage{
ev_icc(data, model = NULL, ...)
}
\arguments{
\item{data}{An \code{evalkit_eval} object carrying a cluster column, or a data
frame passed to \code{\link[=as_eval]{as_eval()}} along with \code{...}.}

\item{model}{Which model to score. Optional when the data holds only one.}

\item{...}{Passed to \code{\link[=as_eval]{as_eval()}} when \code{data} is a plain data frame.}
}
\value{
A single number between 0 and 1, with attribute \code{raw} holding the
uncensored estimate.
}
\description{
Estimates the intra-cluster correlation of evaluation scores using the
one-way random effects analysis of variance estimator. This is the quantity
\code{\link[=ev_power]{ev_power()}} needs in order to size a clustered evaluation, and the quantity
that determines how much \code{\link[=ev_cluster]{ev_cluster()}} will widen an interval.
}
\details{
The estimator is
\code{(MSB - MSW) / (MSB + (m0 - 1) * MSW)}, with \code{m0} the usual unbalanced-design
correction for average cluster size. Sampling noise can push the raw estimate
below zero, in which case it is reported as zero and a note is attached.
}
\examples{
set.seed(3)
passage_skill <- rnorm(30, 0, 1.2)
d <- data.frame(
  q       = 1:300,
  passage = rep(1:30, each = 10),
  correct = rbinom(300, 1, plogis(0.5 + rep(passage_skill, each = 10)))
)
ev_icc(as_eval(d, score = correct, item = q, cluster = passage))

}
\seealso{
Other single model: 
\code{\link{ev_bootstrap}()},
\code{\link{ev_cluster}()},
\code{\link{ev_resample}()},
\code{\link{ev_score}()}
}
\concept{single model}
