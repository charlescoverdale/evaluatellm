% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{ev_power}
\alias{ev_power}
\title{Number of Questions Needed to Detect a Difference}
\usage{
ev_power(
  delta,
  sd_diff = NULL,
  pilot = NULL,
  p_a = NULL,
  p_b = NULL,
  correlation = NULL,
  power = 0.8,
  alpha = 0.05,
  icc = 0,
  cluster_size = 1
)
}
\arguments{
\item{delta}{The difference worth detecting, on the score scale.}

\item{sd_diff}{Standard deviation of the per-item difference.}

\item{pilot}{A result from \code{\link[=ev_paired]{ev_paired()}} to take \code{sd_diff} from.}

\item{p_a, p_b}{Expected scores of the two models, for binary grading.}

\item{correlation}{Expected correlation between the two models' item scores.
Used with \code{p_a} and \code{p_b}. Default \code{0}, which is conservative.}

\item{power}{Target power. Default \code{0.8}.}

\item{alpha}{Two-sided significance level. Default \code{0.05}.}

\item{icc}{Intra-cluster correlation, from \code{\link[=ev_icc]{ev_icc()}}. Default \code{0}.}

\item{cluster_size}{Questions per cluster. Default \code{1}, meaning no clustering.}
}
\value{
An \code{evalkit_power} object with elements \code{n_items}, \code{n_clusters},
\code{delta}, \code{sd_diff}, \code{power}, \code{alpha}, \code{design_effect}, and \code{icc}.
}
\description{
Plans a paired model comparison: given the size of the gain worth detecting
and the variability of the per-item difference, returns how many questions
the evaluation needs.
}
\details{
The calculation is the standard one for a paired mean,
\code{n = (z_alpha/2 + z_beta)^2 * sd_diff^2 / delta^2}, multiplied by the design
effect \code{1 + (cluster_size - 1) * icc} when questions come in clusters. Normal
quantiles are used, which is conventional for planning and slightly
optimistic for very small \code{n}.

Getting \code{sd_diff} right is the whole exercise. It is the standard deviation of
the per-question difference, not of the scores, and it is much smaller than
people expect because the two models face the same questions. The reliable
way to get it is a pilot run passed through \code{\link[=ev_paired]{ev_paired()}} and handed back here
as \code{pilot}. Failing that, supply \code{p_a}, \code{p_b} and a correlation for binary
scoring: a correlation of 0.7 is typical for models of similar capability.
}
\examples{
# Detect a 2 point gain, binary scoring, models correlated at 0.7
ev_power(delta = 0.02, p_a = 0.72, p_b = 0.70, correlation = 0.7)

# The same target, but questions come 8 to a passage
ev_power(delta = 0.02, p_a = 0.72, p_b = 0.70, correlation = 0.7,
         icc = 0.25, cluster_size = 8)

}
\seealso{
Other planning: 
\code{\link{ev_mde}()}
}
\concept{planning}
