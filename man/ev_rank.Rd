% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank.R
\name{ev_rank}
\alias{ev_rank}
\title{Bootstrap Rank Intervals for a Leaderboard}
\usage{
ev_rank(data, R = 2000, level = 0.95, higher_better = TRUE, seed = NULL, ...)
}
\arguments{
\item{data}{An \code{evalkit_eval} object holding several models, or a data frame
passed to \code{\link[=as_eval]{as_eval()}} along with \code{...}.}

\item{R}{Number of bootstrap replicates. Default \code{2000}.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{higher_better}{Logical. Whether a larger score means a better model.
Default \code{TRUE}.}

\item{seed}{Optional integer seed for reproducibility.}

\item{...}{Passed to \code{\link[=as_eval]{as_eval()}} when \code{data} is a plain data frame.}
}
\value{
An \code{evalkit_rank} object with element \code{models}, a data frame ordered
best first with columns \code{model}, \code{estimate}, \code{se}, \code{conf_low}, \code{conf_high},
\code{rank}, \code{rank_low}, \code{rank_high}, and \code{p_best}.
}
\description{
Resamples questions to ask how stable a leaderboard actually is. Returns each
model's score, its rank interval, and the probability it is genuinely the best
model in the table.
}
\details{
Leaderboards are read as though the ordering were a fact, when it is an
estimate like any other. Two models separated by half a point on a
400-question evaluation will often swap places on a different 400 questions.
The rank interval says which orderings the data actually support, and
\code{p_best} says how much confidence the top position deserves.

Clusters are resampled whole where a cluster column exists, matching the
dependence structure that \code{\link[=ev_cluster]{ev_cluster()}} handles analytically.
}
\examples{
set.seed(12)
difficulty <- rnorm(300)
skill <- c(a = 1.0, b = 0.95, c = 0.6, d = 0.2)
d <- do.call(rbind, lapply(names(skill), function(m) {
  data.frame(q = 1:300, m = m,
             correct = rbinom(300, 1, plogis(skill[[m]] - difficulty)))
}))
ev_rank(as_eval(d, score = correct, item = q, model = m), R = 500, seed = 1)

}
\seealso{
Other leaderboard: 
\code{\link{ev_elo}()}
}
\concept{leaderboard}
